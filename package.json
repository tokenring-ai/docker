{
  "name": "@tokenring-ai/docker",
  "description": "Docker integration package for Token Ring AI agents, providing comprehensive Docker operations through a configurable service and tools.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/chat": "0.2.0",
    "zod": "^4.3.6",
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/filesystem": "0.2.0",
    "@tokenring-ai/sandbox": "0.2.0",
    "@tokenring-ai/utility": "0.2.0",
    "@tokenring-ai/terminal": "0.2.0",
    "execa": "^9.6.1",
    "glob-gitignore": "^1.0.15"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "scripts": {
    "eslint": "eslint --fix ./",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "type": "module",
  "devDependencies": {
    "vitest": "^4.0.18",
    "typescript": "^5.9.3"
  }
}
